/*const nome = document.querySelector('input[name="nome"]')
let cpf = formCliente.cpf.value
let celular = formCliente.celular.value
let email = formCliente.email.value

const form = document.querySelector('formCliente')
const validaInput = ()=>{
    if(!nome.value){
        nome.nextElementSibiling.classList.remove('error-hidden')
    }
}

form.addEventListener('submit', (e) => {
    e.preventDefault()
    validaInput()
})


function validar(){

    if (nome === ""){
        alert('Preencha o campo nome')
        formCliente.nome.focus()
        return false
    }else if (cpf === ""){
        alert('Preencha o campo cpf')
        formCliente.cpf.focus()
        return false
    }else if (celular === ""){
        alert('Preencha o campo celular')
        formCliente.celular.focus()
        return false
    }else if (email === ""){
        alert('Preencha o campo email')
        formCliente.email.focus()
        return false
    }else{
        document.forms["formCliente"].submit()
    }
}*/
